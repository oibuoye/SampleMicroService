apiVersion: v1
kind: Service
metadata:
  name: platformnpservice-srv
spec:
  ports:
  - name: platformservice
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: platformservice
  type: NodePort
